<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.assignment4">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Assignment4">
        <activity android:name=".AddToBasket"></activity>
        <activity android:name=".ViewCustomers" />
        <activity android:name=".ViewItems" />
        <activity android:name=".UpdateItem" />
        <activity android:name=".AddItem" />
        <activity android:name=".MyAdapter" />
        <activity android:name=".ViewCustomerInfo" />
        <activity android:name=".SearchResult" />
        <activity android:name=".AdminPage" />
        <activity android:name=".CustomerPage" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>